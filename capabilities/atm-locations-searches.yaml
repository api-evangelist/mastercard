naftiko: 1.0.0-alpha2
info:
  label: Mastercard ATM Locations — Searches
  description: 'Mastercard ATM Locations — Searches. 1 operations. Lead operation: Mastercard Get a List of Atm Details. Self-contained
    Naftiko capability covering one Mastercard business surface.'
  tags:
  - Mastercard
  - Searches
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: atm-locations-searches
    baseUri: https://api.mastercard.com/locations/atms
    description: Mastercard ATM Locations — Searches business capability. Self-contained, no shared references.
    resources:
    - name: searches
      path: /searches
      operations:
      - name: getatms
        method: POST
        description: Mastercard Get a List of Atm Details
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: atm-locations-searches-rest
    port: 8080
    description: REST adapter for Mastercard ATM Locations — Searches. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/searches
      name: searches
      description: REST surface for searches.
      operations:
      - method: POST
        name: getatms
        description: Mastercard Get a List of Atm Details
        call: atm-locations-searches.getatms
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: atm-locations-searches-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard ATM Locations — Searches. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: mastercard-get-list-atm-details
      description: Mastercard Get a List of Atm Details
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: atm-locations-searches.getatms
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
