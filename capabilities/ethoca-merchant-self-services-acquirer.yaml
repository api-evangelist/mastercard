naftiko: 1.0.0-alpha2
info:
  label: Mastercard Ethoca Merchant Self Services — Acquirer
  description: 'Mastercard Ethoca Merchant Self Services — Acquirer. 1 operations. Lead operation: Mastercard Delete Existing
    Acquirer Reference Id and Card Acceptor Id Pair. Self-contained Naftiko capability covering one Mastercard business surface.'
  tags:
  - Mastercard
  - Acquirer
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: ethoca-merchant-self-services-acquirer
    baseUri: https://api.ethocaweb.com/ethoca/merchant-self-services
    description: Mastercard Ethoca Merchant Self Services — Acquirer business capability. Self-contained, no shared references.
    resources:
    - name: sub-merchants-guid-locations-location_id-acquirer-merchant-ids-acquirer_merchant
      path: /sub-merchants/{guid}/locations/{location_id}/acquirer-merchant-ids/{acquirer_merchant_id}
      operations:
      - name: deleteacceptedacquirermerchantid
        method: DELETE
        description: Mastercard Delete Existing Acquirer Reference Id and Card Acceptor Id Pair
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ethoca-merchant-self-services-acquirer-rest
    port: 8080
    description: REST adapter for Mastercard Ethoca Merchant Self Services — Acquirer. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/sub-merchants/{guid}/locations/{location-id}/acquirer-merchant-ids/{acquirer-merchant-id}
      name: sub-merchants-guid-locations-location-id-acquirer-merchant-ids-acquirer-merchant
      description: REST surface for sub-merchants-guid-locations-location_id-acquirer-merchant-ids-acquirer_merchant.
      operations:
      - method: DELETE
        name: deleteacceptedacquirermerchantid
        description: Mastercard Delete Existing Acquirer Reference Id and Card Acceptor Id Pair
        call: ethoca-merchant-self-services-acquirer.deleteacceptedacquirermerchantid
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ethoca-merchant-self-services-acquirer-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Ethoca Merchant Self Services — Acquirer. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: mastercard-delete-existing-acquirer-reference
      description: Mastercard Delete Existing Acquirer Reference Id and Card Acceptor Id Pair
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ethoca-merchant-self-services-acquirer.deleteacceptedacquirermerchantid
      outputParameters:
      - type: object
        mapping: $.
