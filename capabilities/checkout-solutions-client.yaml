naftiko: 1.0.0-alpha2
info:
  label: Mastercard Checkout Solutions — Client
  description: 'Mastercard Checkout Solutions — Client. 2 operations. Lead operation: Mastercard Asynchronous - Dpa Bulk Add/update/delete.
    Self-contained Naftiko capability covering one Mastercard business surface.'
  tags:
  - Mastercard
  - Client
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: checkout-solutions-client
    baseUri: https://sandbox.api.mastercard.com/src/onboarding
    description: Mastercard Checkout Solutions — Client business capability. Self-contained, no shared references.
    resources:
    - name: srci-src-client-id-dpas-batch
      path: /srci/{src-client-id}/dpas/batch
      operations:
      - name: bulkaddupdatedpa
        method: POST
        description: Mastercard Asynchronous - Dpa Bulk Add/update/delete
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: srci-src-client-id-dpas-batch-status-batch-id
      path: /srci/{src-client-id}/dpas/batch/status/{batch-id}
      operations:
      - name: getdpabatchstatus
        method: GET
        description: Mastercard Get Status of the Dpa Add/update Async (batch) Job by Batch Id.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: checkout-solutions-client-rest
    port: 8080
    description: REST adapter for Mastercard Checkout Solutions — Client. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/srci/{src-client-id}/dpas/batch
      name: srci-src-client-id-dpas-batch
      description: REST surface for srci-src-client-id-dpas-batch.
      operations:
      - method: POST
        name: bulkaddupdatedpa
        description: Mastercard Asynchronous - Dpa Bulk Add/update/delete
        call: checkout-solutions-client.bulkaddupdatedpa
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/srci/{src-client-id}/dpas/batch/status/{batch-id}
      name: srci-src-client-id-dpas-batch-status-batch-id
      description: REST surface for srci-src-client-id-dpas-batch-status-batch-id.
      operations:
      - method: GET
        name: getdpabatchstatus
        description: Mastercard Get Status of the Dpa Add/update Async (batch) Job by Batch Id.
        call: checkout-solutions-client.getdpabatchstatus
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: checkout-solutions-client-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Checkout Solutions — Client. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: mastercard-asynchronous-dpa-bulk-add
      description: Mastercard Asynchronous - Dpa Bulk Add/update/delete
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: checkout-solutions-client.bulkaddupdatedpa
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: mastercard-get-status-dpa-add
      description: Mastercard Get Status of the Dpa Add/update Async (batch) Job by Batch Id.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: checkout-solutions-client.getdpabatchstatus
      outputParameters:
      - type: object
        mapping: $.
