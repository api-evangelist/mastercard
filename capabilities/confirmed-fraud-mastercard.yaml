naftiko: 1.0.0-alpha2
info:
  label: Mastercard Confirmed Fraud — Mastercard
  description: 'Mastercard Confirmed Fraud — Mastercard. 2 operations. Lead operation: Add a New Fraud Record Using Minimal
    Input Parameters for Mastercard Built Transactions.. Self-contained Naftiko capability covering one Mastercard business
    surface.'
  tags:
  - Mastercard
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: confirmed-fraud-mastercard
    baseUri: https://api.mastercard.com/fld/confirmed-frauds
    description: Mastercard Confirmed Fraud — Mastercard business capability. Self-contained, no shared references.
    resources:
    - name: mastercard-frauds
      path: /mastercard-frauds
      operations:
      - name: submitmastercardfraud
        method: POST
        description: Add a New Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: updatemastercardfraud
        method: PUT
        description: Change an Existing Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: confirmed-fraud-mastercard-rest
    port: 8080
    description: REST adapter for Mastercard Confirmed Fraud — Mastercard. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/mastercard-frauds
      name: mastercard-frauds
      description: REST surface for mastercard-frauds.
      operations:
      - method: POST
        name: submitmastercardfraud
        description: Add a New Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
        call: confirmed-fraud-mastercard.submitmastercardfraud
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatemastercardfraud
        description: Change an Existing Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
        call: confirmed-fraud-mastercard.updatemastercardfraud
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: confirmed-fraud-mastercard-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Confirmed Fraud — Mastercard. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: add-new-fraud-record-using
      description: Add a New Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: confirmed-fraud-mastercard.submitmastercardfraud
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: change-existing-fraud-record-using
      description: Change an Existing Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: confirmed-fraud-mastercard.updatemastercardfraud
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
