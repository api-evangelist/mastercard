naftiko: 1.0.0-alpha2
info:
  label: Mastercard Checkout Solutions — Status
  description: 'Mastercard Checkout Solutions — Status. 1 operations. Lead operation: Mastercard Get Status of the Dpa Add/update
    Async (batch) Job by Batch Id.. Self-contained Naftiko capability covering one Mastercard business surface.'
  tags:
  - Mastercard
  - Status
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: checkout-solutions-status
    baseUri: https://sandbox.api.mastercard.com/src/onboarding
    description: Mastercard Checkout Solutions — Status business capability. Self-contained, no shared references.
    resources:
    - name: srci-src-client-id-dpas-batch-status-batch-id
      path: /srci/{src-client-id}/dpas/batch/status/{batch-id}
      operations:
      - name: getdpabatchstatus
        method: GET
        description: Mastercard Get Status of the Dpa Add/update Async (batch) Job by Batch Id.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: checkout-solutions-status-rest
    port: 8080
    description: REST adapter for Mastercard Checkout Solutions — Status. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/srci/{src-client-id}/dpas/batch/status/{batch-id}
      name: srci-src-client-id-dpas-batch-status-batch-id
      description: REST surface for srci-src-client-id-dpas-batch-status-batch-id.
      operations:
      - method: GET
        name: getdpabatchstatus
        description: Mastercard Get Status of the Dpa Add/update Async (batch) Job by Batch Id.
        call: checkout-solutions-status.getdpabatchstatus
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: checkout-solutions-status-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Checkout Solutions — Status. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: mastercard-get-status-dpa-add
      description: Mastercard Get Status of the Dpa Add/update Async (batch) Job by Batch Id.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: checkout-solutions-status.getdpabatchstatus
      outputParameters:
      - type: object
        mapping: $.
