naftiko: 1.0.0-alpha2
info:
  label: Mastercard Ethoca Merchant Self Services — Status
  description: 'Mastercard Ethoca Merchant Self Services — Status. 1 operations. Lead operation: Mastercard Change Sub-merchant
    Status.. Self-contained Naftiko capability covering one Mastercard business surface.'
  tags:
  - Mastercard
  - Status
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: ethoca-merchant-self-services-status
    baseUri: https://api.ethocaweb.com/ethoca/merchant-self-services
    description: Mastercard Ethoca Merchant Self Services — Status business capability. Self-contained, no shared references.
    resources:
    - name: alerts-merchants-member_id-sub-merchants-sub_merchant_id-status-changes
      path: /alerts/merchants/{member_id}/sub-merchants/{sub_merchant_id}/status-changes
      operations:
      - name: updatealertssubmerchantstatus
        method: PUT
        description: Mastercard Change Sub-merchant Status.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: ethoca-merchant-self-services-status-rest
    port: 8080
    description: REST adapter for Mastercard Ethoca Merchant Self Services — Status. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/alerts/merchants/{member-id}/sub-merchants/{sub-merchant-id}/status-changes
      name: alerts-merchants-member-id-sub-merchants-sub-merchant-id-status-changes
      description: REST surface for alerts-merchants-member_id-sub-merchants-sub_merchant_id-status-changes.
      operations:
      - method: PUT
        name: updatealertssubmerchantstatus
        description: Mastercard Change Sub-merchant Status.
        call: ethoca-merchant-self-services-status.updatealertssubmerchantstatus
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ethoca-merchant-self-services-status-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Ethoca Merchant Self Services — Status. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: mastercard-change-sub-merchant-status
      description: Mastercard Change Sub-merchant Status.
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ethoca-merchant-self-services-status.updatealertssubmerchantstatus
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
