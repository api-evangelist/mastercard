naftiko: 1.0.0-alpha2
info:
  label: Mastercard Confirmed Fraud — Confirmed  Fraud  Submission
  description: 'Mastercard Confirmed Fraud — Confirmed  Fraud  Submission. 2 operations. Lead operation: Add a New Fraud Record
    Using Complete Input Parameters for Both Mastercard and Issuer Built Transactions.. Self-contained Naftiko capability
    covering one Mastercard business surface.'
  tags:
  - Mastercard
  - Confirmed  Fraud  Submission
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_API_KEY: MASTERCARD_API_KEY
capability:
  consumes:
  - type: http
    namespace: confirmed-fraud-confirmed-fraud-submission
    baseUri: https://api.mastercard.com/fld/confirmed-frauds
    description: Mastercard Confirmed Fraud — Confirmed  Fraud  Submission business capability. Self-contained, no shared
      references.
    resources:
    - name: issuer-frauds
      path: /issuer-frauds
      operations:
      - name: submitissuerfraud
        method: POST
        description: Add a New Fraud Record Using Complete Input Parameters for Both Mastercard and Issuer Built Transactions.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: mastercard-frauds
      path: /mastercard-frauds
      operations:
      - name: submitmastercardfraud
        method: POST
        description: Add a New Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: confirmed-fraud-confirmed-fraud-submission-rest
    port: 8080
    description: REST adapter for Mastercard Confirmed Fraud — Confirmed  Fraud  Submission. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/issuer-frauds
      name: issuer-frauds
      description: REST surface for issuer-frauds.
      operations:
      - method: POST
        name: submitissuerfraud
        description: Add a New Fraud Record Using Complete Input Parameters for Both Mastercard and Issuer Built Transactions.
        call: confirmed-fraud-confirmed-fraud-submission.submitissuerfraud
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/mastercard-frauds
      name: mastercard-frauds
      description: REST surface for mastercard-frauds.
      operations:
      - method: POST
        name: submitmastercardfraud
        description: Add a New Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
        call: confirmed-fraud-confirmed-fraud-submission.submitmastercardfraud
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: confirmed-fraud-confirmed-fraud-submission-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Confirmed Fraud — Confirmed  Fraud  Submission. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: add-new-fraud-record-using
      description: Add a New Fraud Record Using Complete Input Parameters for Both Mastercard and Issuer Built Transactions.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: confirmed-fraud-confirmed-fraud-submission.submitissuerfraud
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: add-new-fraud-record-using-2
      description: Add a New Fraud Record Using Minimal Input Parameters for Mastercard Built Transactions.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: confirmed-fraud-confirmed-fraud-submission.submitmastercardfraud
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
