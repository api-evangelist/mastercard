{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CardAlias",
  "type": "string",
  "description": "Alias assigned to each card. This attribute is available only if the CNA (Card number alias) functionality is enabled for the institution."
}